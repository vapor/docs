matrix:
- name: Markdown
  sources: 'docs/**/*.md'
  default_encoding: utf-8
  aspell:
    lang:
      - en
  pipeline:
  - pyspelling.filters.markdown:
      comments: false
      ignores:
      - code 
      - pre